# JavaGuesser

```
until (!continue){
  get input 0-3
    switch 0:
      exit
    switch 1:
      initialize random number
      while (notFin){
        get guess, compare return result
    switch 2:
      int max = 100, min = 0;
      while (notFin){
        guess ((max+min)/2)
        switch 'l':
          min = ((max+min)/2)
        switch 'h':
          max = ((max+min)/2)
        switch 'c':
          notFin = fin
```
        
